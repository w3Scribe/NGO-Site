import { createFileRoute } from '@tanstack/react-router';
import { Button } from 'components/ui/button';
import { motion } from 'framer-motion';
import { useEffect, useState } from 'react';
import { featuredNGOs, upcomingEvents, homeTestimonials } from '@/constants/homeData';
import { initialStats, targetStats, heroAuraAnimations } from '@/constants/homeAnimations';
import { homepageSections, helpSteps, missionCategories } from '@/constants/uiContent';
import { iconPaths, buttonConfig } from '@/constants/ui-elements';

export const Route = createFileRoute('/')({
  component: RouteComponent,
});

function RouteComponent() {
  const [stats, setStats] = useState(initialStats);

  // Animated counter effect
  useEffect(() => {
    const interval = setInterval(() => {
      setStats((prev) => {
        const newStats = { ...prev };
        if (newStats.volunteers < targetStats.volunteers) newStats.volunteers += 5;
        if (newStats.ngos < targetStats.ngos) newStats.ngos += 1;
        if (newStats.donations < targetStats.donations) newStats.donations += 50000;
        if (newStats.beneficiaries < targetStats.beneficiaries) newStats.beneficiaries += 100;

        // Clear interval when all counters reach their target
        if (
          newStats.volunteers >= targetStats.volunteers &&
          newStats.ngos >= targetStats.ngos &&
          newStats.donations >= targetStats.donations &&
          newStats.beneficiaries >= targetStats.beneficiaries
        ) {
          clearInterval(interval);
        }

        return newStats;
      });
    }, 50);

    return () => clearInterval(interval);
  }, []);

  return (
    <motion.div
      className="min-h-screen bg-white"
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      exit={{ opacity: 0 }}
    >
      {/* Hero Section with Texture/Aura Effect */}
      <motion.section
        className="relative h-[100vh] bg-cover bg-center flex items-center overflow-hidden"
        style={{ backgroundImage: "url('/home.jpg')" }}
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ duration: 0.8 }}
      >
        {/* Overlay with texture */}
        <div className="absolute inset-0 bg-black bg-opacity-40 mix-blend-multiply"></div>

        {/* Noise Texture */}
        <div
          className="absolute inset-0 opacity-10"
          style={{
            backgroundImage: `url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E")`,
            backgroundSize: '200px',
          }}
        ></div>

        {/* Aura/Glow Effects */}
        {heroAuraAnimations.map((aura, index) => (
          <motion.div
            key={index}
            className={aura.position}
            animate={aura.animation}
            transition={aura.transition}
          ></motion.div>
        ))}

        {/* Content */}
        <div className="container mx-auto px-4 relative z-10 pt-10">
          <div className="max-w-3xl text-white">
            <motion.h1
              className="text-6xl md:text-7xl font-bold mb-5"
              initial={{ opacity: 0, y: 30 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{
                duration: 0.8,
                type: 'spring',
                stiffness: 100,
              }}
              style={{ textShadow: '0 4px 8px rgba(0, 0, 0, 0.3)' }}
            >
              Together We Can Make A Difference
            </motion.h1>
            <motion.p
              className="text-xl md:text-2xl mb-10"
              initial={{ opacity: 0, y: 30 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{
                duration: 0.8,
                delay: 0.2,
                type: 'spring',
                stiffness: 100,
              }}
              style={{ textShadow: '0 4px 8px rgba(0, 0, 0, 0.3)' }}
            >
              Join our community of NGOs and volunteers to create positive
              change. Together, we can transform lives and build a better future.
            </motion.p>
            <motion.div
              className="flex flex-wrap gap-3"
              initial={{ opacity: 0, y: 30 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{
                duration: 0.8,
                delay: 0.4,
                type: 'spring',
                stiffness: 100,
              }}
            >
              <Button
                asChild
                className="bg-primary hover:bg-primary/90 text-white transition-all duration-200 group"
                size="lg"
              >
                <a href="/NGOs" className="inline-flex items-center">
                  Explore NGOs
                  <motion.svg
                    xmlns="http://www.w3.org/2000/svg"
                    className="h-5 w-5 ml-1 group-hover:translate-x-1 transition-transform duration-200"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                    initial={{ x: 0 }}
                    animate={{ x: [0, 5, 0] }}
                    transition={{
                      duration: 1.5,
                      repeat: Infinity,
                      repeatType: 'loop',
                      ease: 'easeInOut',
                      delay: 0.5,
                    }}
                  >
                    <path
                      strokeLinecap="round"
                      strokeLinejoin="round"
                      strokeWidth={2}
                      d={iconPaths.arrowRight}
                    />
                  </motion.svg>
                </a>
              </Button>
              <Button
                asChild
                variant="outline"
                className="border border-white text-white bg-transparent hover:bg-white/10 transition-all duration-200"
                size="lg"
              >
                <a href="/contact">Get Involved</a>
              </Button>
            </motion.div>
          </div>
        </div>

        {/* Key Statistics */}
        <div className="absolute bottom-0 left-0 right-0 bg-black/60 backdrop-blur-sm py-6 px-4">
          <div className="container mx-auto">
            <div className="grid grid-cols-2 md:grid-cols-4 gap-6 text-white text-center">
              <div>
                <div className="text-3xl lg:text-5xl font-bold mb-1">
                  {stats.volunteers.toLocaleString()}+
                </div>
                <div className="text-sm lg:text-lg opacity-80">Volunteers</div>
              </div>
              <div>
                <div className="text-3xl lg:text-5xl font-bold mb-1">
                  {stats.ngos.toLocaleString()}+
                </div>
                <div className="text-sm lg:text-lg opacity-80">
                  Registered NGOs
                </div>
              </div>
              <div>
                <div className="text-3xl lg:text-5xl font-bold mb-1">
                  ₹{(stats.donations / 1000000).toFixed(1)}M+
                </div>
                <div className="text-sm lg:text-lg opacity-80">
                  Funds Raised
                </div>
              </div>
              <div>
                <div className="text-3xl lg:text-5xl font-bold mb-1">
                  {stats.beneficiaries.toLocaleString()}+
                </div>
                <div className="text-sm lg:text-lg opacity-80">
                  Lives Impacted
                </div>
              </div>
            </div>
          </div>
        </div>
      </motion.section>

      {/* Mission */}
      <section className="py-20">
        <div className="container mx-auto px-4">
          <div className="text-center mb-16">
            <h2 className="text-3xl font-bold text-gray-800 mb-4">
              {homepageSections.missionSection.title}
            </h2>
            <div className="w-20 h-1 bg-primary mx-auto mb-6"></div>
            <p className="max-w-3xl mx-auto text-lg text-gray-600">
              {homepageSections.missionSection.description}
            </p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8 mt-12">
            {missionCategories.map((category, index) => (
              <div key={index} className="text-center">
                <div className="bg-primary/10 w-20 h-20 rounded-full flex items-center justify-center mx-auto mb-6">
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    className="h-10 w-10 text-primary"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                  >
                    {category.icon === 'book' && (
                      <path
                        strokeLinecap="round"
                        strokeLinejoin="round"
                        strokeWidth={2}
                        d={iconPaths.book}
                      />
                    )}
                    {category.icon === 'heart' && (
                      <path
                        strokeLinecap="round"
                        strokeLinejoin="round"
                        strokeWidth={2}
                        d={iconPaths.heart}
                      />
                    )}
                    {category.icon === 'home' && (
                      <path
                        strokeLinecap="round"
                        strokeLinejoin="round"
                        strokeWidth={2}
                        d={iconPaths.home}
                      />
                    )}
                  </svg>
                </div>
                <h3 className="text-xl font-bold text-gray-800 mb-3">{category.title}</h3>
                <p className="text-gray-600">{category.description}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured NGOs */}
      <section className="py-20 bg-slate-50">
        <div className="container mx-auto px-4">
          <div className="flex justify-between items-end mb-12">
            <div>
              <h2 className="text-3xl font-bold text-gray-800 mb-4">
                {homepageSections.featuredNgosSection.title}
              </h2>
              <div className="w-20 h-1 bg-primary mb-4"></div>
              <p className="text-gray-600 max-w-2xl">
                {homepageSections.featuredNgosSection.description}
              </p>
            </div>
            <Button
              asChild
              variant="ghost"
              className="hidden md:flex items-center text-primary hover:text-primary/90 hover:bg-primary/5 transition-all duration-200 rounded-md"
            >
              <a href="/NGOs" className="flex items-center">
                <span>View All</span>
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  className="h-4 w-4 ml-1"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor"
                >
                  <path
                    strokeLinecap="round"
                    strokeLinejoin="round"
                    strokeWidth={2}
                    d={iconPaths.arrowRight}
                  />
                </svg>
              </a>
            </Button>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {featuredNGOs.map((ngo) => (
              <div
                key={ngo.id}
                className="bg-white rounded-xl shadow-md overflow-hidden transition-transform hover:scale-[1.02] duration-300"
              >
                <div className="h-56 overflow-hidden">
                  <img
                    src={ngo.image}
                    alt={ngo.name}
                    className="w-full h-full object-cover transition-transform hover:scale-110 duration-500"
                  />
                </div>
                <div className="p-6">
                  <div className="text-xs font-semibold text-primary uppercase tracking-wide mb-1">
                    {ngo.category}
                  </div>
                  <h3 className="text-xl font-bold text-gray-800 mb-3">
                    {ngo.name}
                  </h3>
                  <Button
                    asChild
                    variant="ghost"
                    className="w-full text-primary hover:text-primary/90 hover:bg-primary/5 transition-all duration-200 rounded-md mt-4"
                  >
                    <a href="/contact" className="flex items-center justify-center">
                      <span>Learn More</span>
                      <svg
                        xmlns="http://www.w3.org/2000/svg"
                        className="h-4 w-4 ml-1"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                      >
                        <path
                          strokeLinecap="round"
                          strokeLinejoin="round"
                          strokeWidth={2}
                          d={iconPaths.arrowRight}
                        />
                      </svg>
                    </a>
                  </Button>
                </div>
              </div>
            ))}
          </div>

          <div className="flex justify-center mt-12 md:hidden">
            <Button
              asChild
              variant="ghost"
              className="flex items-center text-primary hover:text-primary/90 hover:bg-primary/5 transition-all duration-200 rounded-md"
            >
              <a href="/NGOs" className="flex items-center">
                <span>View All</span>
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  className="h-4 w-4 ml-1"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor"
                >
                  <path
                    strokeLinecap="round"
                    strokeLinejoin="round"
                    strokeWidth={2}
                    d={iconPaths.arrowRight}
                  />
                </svg>
              </a>
            </Button>
          </div>
        </div>
      </section>

      {/* How It Works */}
      <section className="py-20">
        <div className="container mx-auto px-4">
          <div className="text-center mb-16">
            <h2 className="text-3xl font-bold text-gray-800 mb-4">
              {homepageSections.howToHelpSection.title}
            </h2>
            <div className="w-20 h-1 bg-primary mx-auto mb-6"></div>
            <p className="max-w-3xl mx-auto text-lg text-gray-600">
              {homepageSections.howToHelpSection.description}
            </p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {helpSteps.map((step, index) => (
              <div key={index} className="relative">
                <div className="absolute -top-4 -left-4 w-12 h-12 rounded-full bg-primary text-white flex items-center justify-center text-xl font-bold">
                  {step.step}
                </div>
                <div className="bg-white rounded-xl shadow-sm p-8 h-full">
                  <h3 className="text-xl font-bold text-gray-800 mb-4">{step.title}</h3>
                  <p className="text-gray-600 mb-6">
                    {step.description}
                  </p>
                  <Button
                    asChild
                    variant="ghost"
                    className="w-full text-primary hover:text-primary/90 hover:bg-primary/5 transition-all duration-200 rounded-md"
                  >
                    <a 
                      href={step.step === 1 ? "/NGOs" : step.step === 2 ? "/contact" : "#share"}
                      className="flex items-center justify-center"
                    >
                      <span>{step.step === 1 ? "Donate Now" : step.step === 2 ? "Become a Volunteer" : "Share Now"}</span>
                      <svg
                        xmlns="http://www.w3.org/2000/svg"
                        className="h-4 w-4 ml-1"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                      >
                        <path
                          strokeLinecap="round"
                          strokeLinejoin="round"
                          strokeWidth={2}
                          d={iconPaths.arrowForward}
                        />
                      </svg>
                    </a>
                  </Button>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Upcoming Events */}
      <section className="py-20 bg-slate-50">
        <div className="container mx-auto px-4">
          <div className="text-center mb-16">
            <h2 className="text-3xl font-bold text-gray-800 mb-4">
              {homepageSections.upcomingEventsSection.title}
            </h2>
            <div className="w-20 h-1 bg-primary mx-auto mb-6"></div>
            <p className="max-w-3xl mx-auto text-lg text-gray-600">
              {homepageSections.upcomingEventsSection.description}
            </p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {upcomingEvents.map((event) => (
              <div
                key={event.id}
                className="bg-white rounded-xl shadow-md overflow-hidden"
              >
                <div className="h-48 overflow-hidden">
                  <img
                    src={event.image}
                    alt={event.title}
                    className="w-full h-full object-cover"
                  />
                </div>
                <div className="p-6">
                  <h3 className="text-xl font-bold text-gray-800 mb-4">
                    {event.title}
                  </h3>
                  <div className="flex items-center text-gray-600 mb-2">
                    <svg
                      xmlns="http://www.w3.org/2000/svg"
                      className="h-5 w-5 mr-2"
                      fill="none"
                      viewBox="0 0 24 24"
                      stroke="currentColor"
                    >
                      <path
                        strokeLinecap="round"
                        strokeLinejoin="round"
                        strokeWidth={2}
                        d={iconPaths.calendar}
                      />
                    </svg>
                    <span>{event.date}</span>
                  </div>
                  <div className="flex items-center text-gray-600 mb-4">
                    <svg
                      xmlns="http://www.w3.org/2000/svg"
                      className="h-5 w-5 mr-2"
                      fill="none"
                      viewBox="0 0 24 24"
                      stroke="currentColor"
                    >
                      <path
                        strokeLinecap="round"
                        strokeLinejoin="round"
                        strokeWidth={2}
                        d={iconPaths.location}
                      />
                    </svg>
                    <span>{event.location}</span>
                  </div>

                  <Button
                    asChild
                    variant="ghost"
                    className="w-full text-primary hover:text-primary/90 hover:bg-primary/5 transition-all duration-200 rounded-md"
                  >
                    <a href="/contact">Learn More</a>
                  </Button>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Testimonials */}
      <section className="py-20">
        <div className="container mx-auto px-4">
          <div className="text-center mb-16">
            <h2 className="text-3xl font-bold text-gray-800 mb-4">
              {homepageSections.testimonialsSection.title}
            </h2>
            <div className="w-20 h-1 bg-primary mx-auto mb-6"></div>
            <p className="max-w-3xl mx-auto text-lg text-gray-600">
              {homepageSections.testimonialsSection.description}
            </p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {homeTestimonials.map((testimonial) => (
              <div
                key={testimonial.id}
                className="bg-white p-6 rounded-xl shadow-md border border-gray-100"
              >
                <div className="flex items-center mb-4">
                  <div className="w-12 h-12 rounded-full overflow-hidden mr-4">
                    <img
                      src={testimonial.avatar}
                      alt={testimonial.author}
                      className="w-full h-full object-cover"
                    />
                  </div>
                  <div>
                    <h4 className="font-bold text-gray-800">
                      {testimonial.author}
                    </h4>
                    <div className="text-sm text-gray-600">
                      {testimonial.role}
                    </div>
                  </div>
                </div>
                <div className="relative">
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    viewBox="0 0 24 24"
                    className="absolute -top-6 -left-2 w-12 h-12 text-primary opacity-10 transform -scale-x-100"
                  >
                    <path
                      d={iconPaths.quote}
                      transform="scale(1.2) translate(5, 5)"
                    />
                  </svg>
                  <p className="text-gray-700 relative z-10">
                    "{testimonial.quote}"
                  </p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Call to Action */}
      <section className="py-20 bg-primary/10">
        <div className="container mx-auto px-4">
          <div className="max-w-4xl mx-auto text-center">
            <h2 className="text-3xl md:text-4xl font-bold text-gray-800 mb-4">
              Ready to Make a Difference?
            </h2>
            <p className="text-xl text-gray-600 mb-10">
              Join our community today and start contributing to causes that
              matter. Whether you're an NGO, a volunteer, or a donor, together
              we can create positive change.
            </p>
            <div className="flex flex-wrap gap-4 justify-center">
              <Button
                asChild
                className="bg-primary hover:bg-primary/90 text-white transition-all duration-200 group"
                size="lg"
              >
                <a href="/NGOs" className="flex items-center">
                  <span>Donate Now</span>
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    className="h-5 w-5 ml-1 group-hover:translate-x-1 transition-transform duration-200"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                  >
                    <path
                      strokeLinecap="round"
                      strokeLinejoin="round"
                      strokeWidth={2}
                      d={iconPaths.arrowRight}
                    />
                  </svg>
                </a>
              </Button>
              <Button
                asChild
                variant="outline"
                className="border border-primary text-primary bg-transparent hover:bg-primary/10 transition-all duration-200"
                size="lg"
              >
                <a href="/contact">Get Involved</a>
              </Button>
            </div>
          </div>
        </div>
      </section>
    </motion.div>
  );
}
